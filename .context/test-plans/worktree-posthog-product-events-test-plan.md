# Test Plan
Generated by /plan-eng-review on Mar 22, 2026
Branch: worktree-posthog-product-events

## Part 1: PostHog Product Events (already implemented)

### Affected Codepaths
- `src/observability/posthog_events.zig` — 9 new event functions + 2 API error tracking functions
- `src/cmd/serve.zig` — `trackServerStarted` before HTTP listen
- `src/cmd/worker.zig` — `trackWorkerStarted`, `trackStartupFailed` for DB/migration
- `src/http/handlers/workspaces_lifecycle.zig` — `trackWorkspaceCreated`, `trackApiError`
- `src/http/handlers/github_callback.zig` — `trackWorkspaceGithubConnected`
- `src/http/handlers/auth_sessions_http.zig` — `trackAuthLoginCompleted`, `writeAuthErrorWithTracking`
- `src/http/handlers/common.zig` — `writeAuthErrorWithTracking` (new), `writeAuthError` delegates
- `src/http/handlers/workspaces_billing.zig` — `trackApiErrorWithContext` for billing errors
- `src/http/handlers/runs/start.zig` — `writeAuthErrorWithTracking`

### Key Interactions to Verify
- All 22 event functions are no-op when PostHog client is null
- Props helper functions return correct field counts and values
- `writeAuthError` backward compat (delegates to new function with null client)

### Edge Cases
- Empty distinct_id falls back to "system" via `distinctIdOrSystem`
- PostHog client is null (POSTHOG_API_KEY unset) — all tracking silently no-ops
- Startup failure path: PostHog client deinit before process.exit

### Critical Paths
- `zig build test` — all 332 tests pass
- `make memleak` — no leaks
- `check-pg-drain.py` — all 168 files pass

## Part 2: Test Target Rename (to implement)

### Affected Files
- `make/test.mk` — `test` target definition change
- `make/test-unit.mk` — remove `test-unit` top-level target
- `.github/workflows/test-unit.yml` — rename to `test.yml`
- `.github/workflows/test.yml` — delete (redundant)
- `README.md` — update `make test` description
- `docs/ZIG_RULES.md` — update references

### Key Interactions to Verify
- `make test` runs: test-zombied + test-unit-zombiectl + test-unit-website + test-unit-app
- `make test-unit` no longer exists (clean error)
- `make qa` still includes `_test_e2e` (unchanged)
- CI: renamed test.yml triggers on PR + push to main

### Edge Cases
- `make test-e2e` still works independently
- `make qa-smoke` still works independently
- `make memleak` not affected

### Critical Paths
- `make test` locally produces same output as old `make test-unit`
- CI workflow file rename preserves step structure + codecov uploads
