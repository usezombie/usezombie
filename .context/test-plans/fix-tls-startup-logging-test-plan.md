# Test Plan
Generated by /plan-eng-review on Mar 22, 2026
Branch: fix/tls-startup-logging

## Affected Files
- `src/db/pool.zig` — parseUrl TLS + query string stripping
- `src/db/pool_test.zig` — new test cases
- `src/cmd/serve.zig` — startup phase logging + structured errors
- `src/cmd/doctor.zig` — startup phase logging + structured errors
- `src/cmd/worker.zig` — startup phase logging + structured errors
- `build.zig` — OpenSSL linking + debug info retention
- `deploy/fly/zombied-dev/fly.toml` — http_service block
- `.github/workflows/deploy-dev.yml` — libssl-dev install

## Key Tests to Verify

### pool.zig parseUrl changes (unit tests in pool_test.zig)
- URL with `?sslmode=require` → dbname is clean, `.tls = .require`
- URL without query params → dbname is clean, `.tls = .require`
- URL with multiple query params `?sslmode=require&application_name=worker` → dbname is clean

### Build verification
- `zig build -Doptimize=ReleaseSafe -Dtarget=x86_64-linux` succeeds with OpenSSL
- `zig build test` passes (all existing + new tests)

### Deploy verification (manual, on Fly.io)
- `fly deploy` succeeds with updated fly.toml
- Machine auto-starts on /healthz request via Cloudflare Tunnel
- Fly logs show `phase=db_connect role=api status=ok` (not panic)
- Fly logs show `phase=redis_connect role=api status=ok`
- `/healthz` returns 200
- `/readyz` returns `{"ready": true}`

## Edge Cases
- PlanetScale connection string with `?sslmode=require` already in vault URL
- PlanetScale connection string with no query params (current state)
- DB unreachable at startup → structured log.err with phase/role/err, not panic

## Critical Paths
- zig build with OpenSSL → Docker image → fly deploy → TLS handshake to PlanetScale → /healthz green
