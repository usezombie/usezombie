# v0.4.0

**Date:** Apr 06, 2026
**Milestones:** M21_001, M22_001, M28_001, M31_001
**Spec:** M21_001_AGENT_INTERRUPT_AND_STEER.md, M22_001_SSE_STREAM_CORRECTNESS.md, M28_001_AGENT_RUN_OBSERVABILITY.md, M31_001_SCHEMA_STANDARDIZATION.md

## What changed

### M28_001 — Agent Run Observability

- Every agent run now produces a complete trace tree in Grafana Tempo — a root `run.execute` span wraps all agent calls and gate tool checks as child spans. Query `{run.id="<id>"}` for a full waterfall.
- Per-workspace Prometheus metrics: token consumption, run outcomes (completed/blocked), and gate repair loops are now broken down by `workspace_id`. Operators can answer "which workspace burns the most tokens?" from Grafana.
- Gate repair loop distribution is now a histogram (`zombie_gate_repair_loops_per_run`) — see p50/p95/p99 of how many repair loops runs need.
- New CLI command: `zombiectl workspace billing --workspace-id <id>` shows a billing breakdown (completed, non-billable, score-gated) with optional `--period` and `--json` flags.
- Importable Grafana dashboard (`docs/grafana/agent_run_breakdown.json`) with 7 panels covering token usage, run outcomes, gate loops, score-gated rates, and top-N runs.
- Grafana setup playbook (`playbooks/M28_001_GRAFANA_OBSERVABILITY.md`) with gate scripts for credential check, Prometheus verification, and dashboard import.
- OpenAPI spec updated with `GET /v1/workspaces/{workspace_id}/billing/summary` endpoint definition.

### M21_001 — Agent Interrupt and Steer

- Users can now steer a running agent mid-run without aborting it via `POST /v1/runs/{id}:interrupt`
- Two interrupt modes: **queued** (delivered at next gate checkpoint) and **instant** (IPC delivery, falls back to queued in v1)
- New `ABORTED` terminal state for explicit abort flows (`/stop` from CLI, abort from UI)
- CLI command: `zombiectl runs interrupt <run_id> <message> [--mode=queued|instant]`
- SSE stream emits `interrupt_ack` event when the server receives an interrupt
- Watch mode (`--watch`) displays `-> steered (mode: queued)` inline in the transcript on interrupt receipt
- Core schema bootstrap was split from `001_initial.sql` into `001_core_foundation.sql`, `002_core_workflow.sql`, and `003_core_results_events.sql` for deterministic, single-concern migration files.
- `core.platform_llm_keys` now uses app-generated UUIDv7 IDs and BIGINT millisecond timestamps.
- `core.tenants` and `core.workspace_memories` now include `updated_at`; `core.gate_results` is enforced append-only via trigger.

### M22_001 — SSE Stream Correctness

- `zombied run --watch` now streams gate results in real time instead of buffering until run completion
- SSE heartbeat fires reliably every 20s even when gates take >20s (prevents proxy connection drops; interval kept below the 25s SO_RCVTIMEO on the pub/sub socket)
- Reconnect with `Last-Event-ID` replays only missed events — no duplicate floods
- Run completing during the subscribe window no longer causes the stream to hang indefinitely
- `zombiectl run --spec <file> --watch` streams gate results with retry, Ctrl+C support, and idle progress indicator
- `zombiectl runs replay <run_id>` prints a per-gate narrative for completed runs

## Technical details

### M28_001
- Root span uses `ctx.trace_id` (matching children) + `root_tc.span_id` in `executeRun()`, closed in `defer` block
- Per-workspace metrics: lock-free bounded hash map (`metrics_workspace.zig`, 4096 slots, open addressing with CAS + ready flag)
- Gate loop histogram: custom `GateLoopBuckets` [0,1,2,3,5,10]
- Billing summary API groups `usage_ledger` by `lifecycle_event`, joins `agent_run_scores` for avg score

### M21_001
- Interrupt messages stored in Redis with `run:interrupt:{run_id}` key prefix, 300s TTL, last-write-wins
- Gate loop polls Redis via `GETDEL` (atomic read-and-delete) at the top of each iteration
- State machine: ABORTED transitions allowed from all active states
- 4 new Prometheus counters: `interrupt_queued_total`, `interrupt_instant_total`, `interrupt_fallback_total`, `run_aborted_total`

### M22_001
- §1: CLI uses `client.open()` + `req.reader()` incremental read loop instead of buffered `client.fetch()`
- §2: `SO_RCVTIMEO` (25s) on Redis pub/sub socket; `readMessage()` returns null on timeout, heartbeat loop checks elapsed time. Heartbeat interval set to 20s (< 25s SO_RCVTIMEO < 30s proxy idle) to guarantee the first wakeup fires before the proxy drops the connection.
- §3: Unified SSE event ID namespace — `created_at` (Unix ms) used for both stored replay and live events; worker publishes `created_at` in gate event JSON
- §4: Post-subscribe race fix — re-query run state after `subscriber.subscribe()`, replay missed events if terminal
- §5: npm CLI `run_watch.js` module with `AbortController`-based SIGINT handling, 3 retries with `Last-Event-ID`, 30s idle progress indicator
- §6: `sendCommand` in `redis_pubsub.zig` now flushes the underlying socket writer after TLS encryption (`stream_writer.interface.flush()`). Previously, AUTH and SUBSCRIBE commands were encrypted into the TLS buffer but never written to the socket — blocking `readRespValue` indefinitely in CI where the pub/sub TLS cert was first available.
- §7: CI workflow adds an explicit "Extract Redis TLS CA cert" step (via `docker compose cp` + `$GITHUB_ENV`) before `make test-integration`, replacing the unreliable `$(shell ...)` Makefile parse-time expansion.
- §8: `zombiectl runs replay` reads `gate_results` field from the server response (was incorrectly reading `gates`, which is always undefined).


### M31_001
- `agent_id` added to `GateLoopConfig` and all interrupt log lines for full observability traceability (run -> workspace -> agent)
- Error codes: `UZ-RUN-008` (Redis store failure), `UZ-RUN-009` (run not in interruptible state)
- Input validation: 4096 byte message limit, mode enum validation, workspace auth gating
- Migration ordering was slid to `004..021`, and migration registration was updated in `schema/embed.zig` and `src/cmd/common.zig`.
- `generatePlatformLlmKeyId()` was added and platform-key write paths/tests were updated to `std.time.milliTimestamp()`.
- `docs/contributing/SCHEMA_CONVENTIONS.md` now documents ID, timestamp, standard-column, audit, and RNG rules.

## Breaking changes

- SSE `id:` field on live events changed from sequential counter to `created_at` Unix milliseconds. Clients parsing `Last-Event-ID` as a sequence number must update.

## Migration

- No migration needed — all changes are additive.
