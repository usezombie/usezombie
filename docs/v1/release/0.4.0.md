# v0.4.0

**Date:** Apr 05, 2026
**Milestone:** M28_001
**Spec:** M28_001_AGENT_RUN_OBSERVABILITY.md

## What changed

- Every agent run now produces a complete trace tree in Grafana Tempo — a root `run.execute` span wraps all agent calls and gate tool checks as child spans. Query `{run.id="<id>"}` for a full waterfall.
- Per-workspace Prometheus metrics: token consumption, run outcomes (completed/blocked), and gate repair loops are now broken down by `workspace_id`. Operators can answer "which workspace burns the most tokens?" from Grafana.
- Gate repair loop distribution is now a histogram (`zombie_gate_repair_loops_per_run`) — see p50/p95/p99 of how many repair loops runs need.
- New CLI command: `zombiectl workspace billing --workspace-id <id>` shows a billing breakdown (completed, non-billable, score-gated) with optional `--period` and `--json` flags.
- Importable Grafana dashboard (`docs/grafana/agent_run_breakdown.json`) with 7 panels covering token usage, run outcomes, gate loops, score-gated rates, and top-N runs.
- Grafana setup playbook (`playbooks/M28_001_GRAFANA_OBSERVABILITY.md`) with gate scripts for credential check, Prometheus verification, and dashboard import.

## Technical details

- Root span uses `TraceContext.generate()` in `executeRun()`, closed in a `defer` block so all exit paths (done, blocked, cancelled, error) emit the span.
- Per-workspace metrics use a lock-free bounded hash map (`metrics_workspace.zig`, 4096 slots, open addressing with CAS). Overflow routes to `_other` bucket with `zombie_workspace_metrics_overflow_total` counter.
- Gate loop histogram uses custom `GateLoopBuckets` [0,1,2,3,5,10] separate from duration buckets.
- Billing summary API (`GET /v1/workspaces/:id/billing/summary`) groups `usage_ledger` by `lifecycle_event`, joins `agent_run_scores` for avg score on gated runs.
- OTel trace ring buffer drain on shutdown (5s timeout) ensures spans are flushed before process exit on deploy.

## Breaking changes

- None

## Migration

- No migration needed — all changes are additive (new metrics, new spans, new API endpoint, new CLI command).
- Existing Prometheus dashboards continue to work — per-workspace metrics are new counters alongside existing global ones.
