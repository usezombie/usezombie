# M31_001: Schema Standardization

**Prototype:** v1.0.0
**Milestone:** M31
**Workstream:** 001
**Date:** Apr 05, 2026
**Status:** IN_PROGRESS
**Branch:** feat/m31-schema-standardization
**Priority:** P2 — consistency debt, no production breakage
**Batch:** B2
**Depends on:** None

---

## Overview

Schema audit (Apr 05, 2026) revealed inconsistencies across 34 tables in 4 schemas. Since tables are rebuilt from scratch (no ALTER migrations), these can be fixed in a single pass.

---

## 1.0 Standardize `platform_llm_keys` Table

**Status:** PENDING

`core.platform_llm_keys` uses `gen_random_uuid()` (UUIDv4) and `TIMESTAMPTZ DEFAULT now()` instead of the project standard: UUIDv7 + BIGINT milliseconds.

**Dimensions:**
- 1.1 PENDING Change `id UUID PRIMARY KEY DEFAULT gen_random_uuid()` to `id UUID PRIMARY KEY` (UUIDv7 generated by application)
- 1.2 PENDING Change `created_at TIMESTAMPTZ NOT NULL DEFAULT now()` to `created_at BIGINT NOT NULL`
- 1.3 PENDING Change `updated_at TIMESTAMPTZ NOT NULL DEFAULT now()` to `updated_at BIGINT NOT NULL`
- 1.4 PENDING Add `generatePlatformLlmKeyId()` to `src/types/id_format.zig`
- 1.5 PENDING Update all INSERT/SELECT paths to use BIGINT milliTimestamp

---

## 2.0 Add Missing `updated_at` Columns

**Status:** PENDING

13 tables lack `updated_at`. Append-only/event tables (where UPDATE is blocked by trigger) are exempt. Mutable tables should have `updated_at`.

**Tables needing `updated_at` (mutable, not append-only):**
- 2.1 PENDING `core.tenants` — mutable (name, api_key_hash can change)
- 2.2 PENDING `core.gate_results` — immutable in practice, but no trigger blocks UPDATE. Add `updated_at` or add append-only trigger.
- 2.3 PENDING `core.workspace_memories` — has `expires_at` which implies mutation. Add `updated_at`.

**Tables exempt (append-only or event-sourced):**
- `core.policy_events` — uses `ts`, append-only by design
- `billing.usage_ledger` — append-only ledger
- `billing.entitlement_policy_audit_snapshots` — audit log
- `billing.workspace_billing_audit` — audit log
- `billing.workspace_credit_audit` — audit log
- `agent.config_linkage_audit_artifacts` — has append-only trigger
- `agent.workspace_latency_baseline` — uses `computed_at`, replace-on-compute pattern
- `agent.agent_run_scores` — uses `scored_at`, write-once per run
- `agent.agent_run_analysis` — uses `analyzed_at`, write-once per run
- `audit.ops_ro_access_events` — uses `accessed_at`, audit log

---

## 3.0 Audit Trail: `updated_by` Column

**Status:** PENDING

No table has `updated_by`. Changes are tracked via `actor` in separate event/transition tables. Evaluate whether direct-mutation tables should have `updated_by`.

**Dimensions:**
- 3.1 PENDING Evaluate: should `core.workspaces`, `core.specs`, `core.runs` have `updated_by TEXT`?
- 3.2 PENDING If yes: add column, populate from context in all UPDATE paths
- 3.3 PENDING If no: document the decision and the event-sourced audit pattern in `docs/contributing/SCHEMA_CONVENTIONS.md`

---

## 4.0 Document Schema Conventions

**Status:** PENDING

Create `docs/contributing/SCHEMA_CONVENTIONS.md` as the canonical reference for:

- 4.1 PENDING ID format: UUIDv7 via `src/types/id_format.zig`, never `gen_random_uuid()`
- 4.2 PENDING Timestamps: BIGINT milliseconds via `std.time.milliTimestamp()`, never TIMESTAMPTZ
- 4.3 PENDING Standard columns: `id`, `created_at`, `updated_at` (for mutable tables)
- 4.4 PENDING Audit pattern: `actor TEXT` in event tables, append-only triggers where needed
- 4.5 PENDING RNG: `std.crypto.random` only, never custom

---

## 5.0 Acceptance Criteria

**Status:** PENDING

- [ ] 5.1 `platform_llm_keys` uses UUIDv7 + BIGINT timestamps
- [ ] 5.2 `tenants`, `gate_results`, `workspace_memories` have `updated_at`
- [ ] 5.3 `docs/contributing/SCHEMA_CONVENTIONS.md` exists and is complete
- [ ] 5.4 `make test` and `make test-integration-db` pass
- [ ] 5.5 No `gen_random_uuid()` or `TIMESTAMPTZ DEFAULT now()` in any migration file

---

## 6.0 Out of Scope

- Adding `updated_by` to existing rows retroactively
- Changing append-only/event tables to add `updated_at`
- Migrating existing data (tables rebuilt from scratch)
