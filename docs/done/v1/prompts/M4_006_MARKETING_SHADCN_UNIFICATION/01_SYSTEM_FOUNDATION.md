# 01 System Foundation Prompt

Goal: establish shared shadcn-style UI source for `website/` and `app/`.

Deliverables:
- shared UI package location and import contract
- component ownership map (shared vs app-specific)
- migration-ready dependency contract
