# 02 Token Contract Prompt

Goal: one token source inside `packages/ui` that emits CSS variables and TS exports.

Deliverables:
- canonical token schema
- CSS variable output contract
- TypeScript token export contract
- drift prevention rule
