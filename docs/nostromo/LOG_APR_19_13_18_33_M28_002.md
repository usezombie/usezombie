# Ripley's Log — Apr 19, 2026: 1:18 PM — M28_002 retire + M28_003 close

## Context

User asked me to implement `docs/v2/pending/P1_API_M28_002_OPENAPI_SPLIT_AND_SVIX_DOCS.md`
and also to move the stale active spec `docs/v2/active/P2_API_M28_003_WEBHOOK_E2E_INTEGRATION_TESTS.md`
(said to be merged) into `docs/v2/done/` in the same PR.

## What I found before writing any code

Surveying the repo against the M28_002 spec's acceptance criteria:

1. **OpenAPI split already landed.** `public/openapi/` exists as a full tree:
   - `root.yaml` — info, servers, tags, security, paths map with `$ref`s.
   - `paths/*.yaml` — one file per tag (webhooks.yaml, zombies.yaml, ...).
   - `components/{schemas,responses,security}.yaml`.
   - `AGENTS.md` explicitly states `public/openapi.json` is a build artifact and
     must not be hand-edited; CI bundle-in-sync gate enforces this.
   - Four agent-edit recipes documented (rename, append, remove, update).
2. **Svix webhook endpoint already documented.**
   - `public/openapi/paths/webhooks.yaml:106` — `/v1/webhooks/svix/{zombie_id}`
     POST operation with `svix-id`/`svix-timestamp`/`svix-signature` header
     parameters, `202 accepted` / 400 / 401 / 404 responses.
   - `public/openapi/root.yaml:135` — `$ref` wired in.
3. **Provenance of the work.** Commit `bf8f735c` —
   `feat(m28_003): OpenAPI split, bundle, sync gate, and agent-edit recipes (#232)`
   under `docs/v2/done/P2_INFRA_M28_003_OPENAPI_TOOLING.md`.
4. **Numbering reshuffled.** The M28_002 slot was reused for Tenant API Key
   Management (`docs/v2/done/P1_API_M28_002_TENANT_API_KEY_MANAGEMENT.md`).
   The OpenAPI-tooling work was renumbered to M28_003 under a different P2 spec.
   This pending M28_002 spec was the original draft that the renumber
   superseded.

## Decision

Asked the user to confirm rather than silently implementing — the spec's work
is already shipped; doing it again would either be a no-op or would churn
already-working files. User confirmed (A + C): retire the obsolete spec and
close out the merged E2E spec.

## What I did

- `git mv docs/v2/active/P2_API_M28_003_WEBHOOK_E2E_INTEGRATION_TESTS.md docs/v2/done/`
  and flipped `Status: IN_PROGRESS` → `Status: DONE`. Content otherwise
  untouched — the shipped work is recorded faithfully in the spec body.
- `git mv docs/v2/pending/P1_API_M28_002_OPENAPI_SPLIT_AND_SVIX_DOCS.md docs/v2/done/`
  and flipped `Status: PENDING` → `Status: DONE (absorbed by
  P2_INFRA_M28_003_OPENAPI_TOOLING)`. Added a "Retirement note" at the top
  pointing to the commit and spec that actually delivered the work, and
  documenting the M28 numbering reshuffle so future readers don't chase ghosts.
- Both moves in the same PR on branch `chore/m28-002-retire` in an isolated
  worktree at `../usezombie-m28-002-retire`.

## What I did not do

- No code changes. No `public/openapi/**` edits, no `make openapi` run
  (the bundle is already in sync — that's M28_003's CI gate's job to enforce).
- No changelog entry. Retirement is internal bookkeeping; no user-visible
  change. The real user-visible release (OpenAPI split + Svix endpoint docs)
  already shipped with M28_003's `<Update>` block.
- No orphan sweep needed — only spec files moved.

## Follow-ups

None. If a future M28_002-flavoured spec is needed, reuse the filename
freely — both prior claimants now live under `done/`.
