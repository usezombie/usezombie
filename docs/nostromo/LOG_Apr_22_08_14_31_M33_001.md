# Ripley's Log — Apr 22, 2026: 08:14 AM — M33_001 (Homelab Zombie)

## Session framing

Kicked off M33_001 (Homelab Zombie — flagship executable sample) on
request, with explicit instruction to ask questions before starting.
The spec as landed had several contradictions and dependency gaps that
made "just start" risky; I surfaced a blocker set before touching
anything, got answers, then opened the workstream.

## Questions I surfaced (before any edit)

1. `zombiectl zombie install --from <path>` does not exist today. M19_001
   owns it, still PENDING. How to proceed — park, widen scope, or
   land-static-and-defer-test?
2. Clawhub SKILL.md frontmatter is not what the spec assumes. The only
   landed sample (`samples/lead-collector/`) uses a different shape than
   the brainstormed sub-skills, and both differ from what the spec
   prescribes (model field, tools list referencing skill names,
   `{{credential.X}}` templating). Who owns reconciliation?
3. Firewall architecture mismatch. `src/zombie/firewall/firewall.zig`
   enforces at the HTTP boundary (`OutboundRequest{tool, method, domain,
   path, body}` → domain + endpoint + injection layers). Spec §3 asserts
   runtime rejection of shell verbs like `kubectl delete`. That is a
   different layer. Where should the enforcement live?
4. Test path — spec prescribes `tests/integration/`, repo uses
   colocation (`src/**/*_integration_test.zig`). Amend?
5. Who owns the `git mv` of the brainstormed sub-skills — header said
   "M32 §9", execution plan said "M33 owns this (see §0.1 below)"
   (with no §0.1 target). Internal contradiction.
6. Dangling `§0.1` reference — clean up in amend?
7. Worktree — confirm creation before any code.

## Decisions (from the user)

1. Path (c): ship static artifacts now; mark §4.x Dimensions
   `BLOCKED_ON: M19_001`; when all remaining dims complete, the spec
   moves to DONE. Another agent is on M19_001.
2. Schema reconciliation is owned by another agent via the M32_001 /
   M33_001 coordination — I stay out of that question. I use the shape
   the spec prescribes for M33's flagship and sub-skill files, and let
   that agent reconcile upstream.
3. Tool enforcement will eventually be supported by nullclaw. For M33
   now, the declarative policy lands in the sub-skill `SKILL.md` files;
   the enforcement layer reads it when it ships. §3 dims are tagged
   `BLOCKED_ON: nullclaw-tool-policy`.
4. Amend the test path to repo convention.
5. M33 owns the landed copies at the new path.
6. Version governed by `VERSION` and sequential changelog. Since M33 is
   parked, no `<Update>` block or VERSION bump in this pass.
7. Yes, create worktree once answers are understood.

## What I did

- CHORE(open): created worktree `../usezombie-m33-homelab-zombie` on
  branch `feat/m33-homelab-zombie`. Moved spec `pending/` → `active/`.
  Amended spec for all the findings above. Added a `## Discovery`
  section so the findings survive this pass.
  - Quirk: the first `git add` + commit captured only the rename, not
    the content edits. Rather than amend (CLAUDE.md forbids implicit
    amend), I landed the amendments as a second commit.
- Authored the two sub-skills (`kubectl-readonly/SKILL.md`,
  `docker-readonly/SKILL.md`) with explicit allowlists in the
  frontmatter `policy:` block. `denied_resources: [secrets]` on the
  kubectl skill, per the spec invariant.
- Authored the flagship `SKILL.md` — model `claude-sonnet-4-6`,
  `skills: [kubectl-readonly, docker-readonly]`, credentials
  `kubectl_config` + `docker_socket`, prompt body covering reasoning
  style and output format.
- Authored `TRIGGER.md` — webhook trigger with payload schema
  `{message: string, 1..4000}` and optional cron `0 9 * * *`.
- Authored `README.md` — operator quickstart, Jellyfin/OOMKilled
  example conversation, firewall allowlist documentation,
  placeholder-credential explanation in two paragraphs. Under 200
  lines.
- Ran eval gates E1–E7 + E10 (everything not dependent on M19_001).
  All green. E8/E9 deferred with the install-from-path handler.

## Dead ends and deferred follow-ups

- I did not create a `tests/integration/` directory. The spec
  prescribed it; the repo doesn't use that convention; user confirmed
  amendment. When M19_001 lands `--from <path>`, the integration test
  colocates with that handler (e.g.
  `src/samples/homelab/install_integration_test.zig` or wherever the
  install-from-path code ends up).
- I did not attempt to extend the Clawhub schema or change the skill
  parser. Those are M2_002 territory; user said another agent owns
  reconciliation.
- I did not `git mv` the brainstormed sub-skills. They are untracked
  in git, so `git mv` is a no-op that would error out. I authored
  fresh `SKILL.md` files at the target path using the brainstormed
  READMEs as reference. The brainstormed directory remains on disk as
  untracked reference material; cleanup (if desired) is a separate,
  non-blocking operation.
- I did not add a `changelog.mdx` `<Update>` block or bump `VERSION`.
  The workstream is parked midway (§3/§4 remain); per the CHORE(close)
  rules, changelog is skipped for parked branches. Those land when
  M19_001 and nullclaw-tool-policy unblock the remaining dims.
- No PR opened — the user asked me to "start on the implementation",
  not to ship.

## Evidence

- Branch: `feat/m33-homelab-zombie` (worktree at
  `~/Projects/usezombie-m33-homelab-zombie`).
- Commits (this session):
  - `chore(m33): open workstream — spec pending→active, amend for repo reality`
  - `chore(m33): apply spec amendments (dims + discovery)`
  - `feat(samples): add homelab flagship zombie (read-only, policy-only pass)`
  - `chore(m33): close as parked — mark dims + ripley log` (this commit)
- Eval results (all landed dims): ✅ E1, E2, E3, E4, E5, E6, E7, E10.
  Deferred: E8 (integration test), E9 (`zombiectl zombie install`
  CLI).
- Gitleaks: clean on both the pre-commit stage and the full worktree
  (no leaks).

## Status

Spec stays in `docs/v2/active/` as `IN_PROGRESS`. §1/§2/§5 all dims
`DONE`. §3/§4 dims `BLOCKED_ON` with explicit unblock conditions.
Ready to move to `done/` when (a) M19_001 ships install-from-path and
its integration test is added, and (b) the nullclaw tool-enforcement
layer lands and §3 dims are exercised end-to-end.
