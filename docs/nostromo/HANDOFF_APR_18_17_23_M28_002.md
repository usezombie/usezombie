# Handoff — M28_002 Tenant API Key Management (parked after §0 rename + M29 specs drafted)

**Date:** Apr 18, 2026: 05:23 PM UTC
**Worktree:** `/Users/kishore/Projects/usezombie-m28-api-keys`
**Branch:** `feat/m28-api-keys`
**PR:** [#222](https://github.com/usezombie/usezombie/pull/222) — `spec: M28_002 Tenant API Key Management` — OPEN, state BLOCKED (branch protection; needs reviewer)
**HEAD:** `c7444054` spec: add M29_001 doc rewrite + M29_002 remove v1 spec/run flow (pending)

---

## Scope / Status

This session continued from the prior handoff (`HANDOFF_APR_18_09_22_00_M28_002.md`, now deleted). The prior session closed with the spec at "10/10 ready" but zero code written. This session shipped **§0 rename** end-to-end (code + tests + spec amendments) plus a handful of ancillary cleanups that landed on the same branch, plus two fresh **pending** workstream specs for milestone M29. The core **M28_002 tenant-key feature (§1-§5)** is still unstarted — steps 4-13 of the execution plan remain PENDING.

- ✅ **M28_002 §0 — Rename `external_agents` → `agent_keys`** — complete at the code layer. `core.external_agents` → `core.agent_keys`; handler file, route enum variants, URL path, log scope, event names all renamed. Zombiectl URL updated (file + subcommand rename deferred to CLI milestone). `zig build && zig build test` green. Orphan sweep returns zero non-archaeology hits. Integration-level verification (Dims 0.1-0.4) still PENDING because they require a live DB run (`make test-integration`) that was not performed in this session. Dim 0.5 (orphan sweep) is DONE.
- ✅ **Spec amendments for deferred scope.** `last_used_at` async stamping was pinned in the prior session to an `httpz` post-flush hook; user directed that the stamping be deferred entirely, with the intended design (bounded Redis queue drained by a zombie worker) captured in a Deferred note. Five dependent rows across §5 Observability, Failure Modes, Leak Tests, and Performance Tests were annotated **(DEFERRED)** inline. Entropy-collision test sizing was also amended: 100k in Debug, 1M in ReleaseFast, wired into `make test` (no separate `make test-entropy` target).
- ✅ **Stale handoff deletion.** `docs/nostromo/HANDOFF_APR_16_01_29_03.md` removed; rolled into the Step 1 commit per user direction.
- ✅ **Test file rename pass.** Ten test files renamed from `mN_NNN_*_test.zig` → purpose-based names. Imports in `src/main.zig`, `src/zombie/metering.zig`, `src/http/byok_http_integration_test.zig` updated; cross-reference comments in the two memory-handler tests updated. Purpose-based names chosen by reading the content of each test file. No behavior change.
- ✅ **RULE FLL extended** in dotfiles and usezombie's hardlinked `docs/greptile-learnings/RULES.md`. Added a **≤ 15 non-test source files per directory** clause (recursive), with a runnable folder-count shell snippet. Scope limited to `usezombie/` repos; docs-only repos exempt.
- ✅ **Milestone-ref scrub** in `dotfiles/AGENTS.md`. Every literal milestone reference (`M28_001 postmortem`, `M11_003`, `M7`, `M7_002`, `M12_001`, `M15`, `M28_002`) replaced with placeholder forms (`M{N}`, `M{N}_{WS}`, `M{N}_{WKSTRM}`) so the Terminology rule now enforces itself against its own governing file. `AGENTS_POLICY_APPENDIX.md` already clean. Both committed and pushed to dotfiles `master` (commit `841c340`).
- ✅ **M29 pending specs drafted and committed** to this branch (per user direction: "append the spec to this PR since we are almost done here"): `P0_CLI_M29_001_DOC_REWRITE_SPEC_TO_ZOMBIE.md` (378 lines) and `P1_API_CLI_M29_002_REMOVE_V1_SPEC_RUN_FLOW.md` (410 lines). Both in `docs/v2/pending/`, Status: PENDING.

### What's NOT done

- ⏳ **M28_002 Execution Plan Steps 4-13** — the entire tenant-API-key feature surface. Schema `030_api_keys.sql`, error codes `ERR_APIKEY_*`, `tenant_api_key.zig` middleware, `bearer_or_api_key.zig` wiring, `api_keys.zig` CRUD handlers, `/v1/api-keys` route registration, integration tests, bootstrap log warning, old-path dead-code sweep, final cross-compile + lint + gitleaks gate. See the spec's Execution Plan for the full 13-step ordering.
- ⏳ **§0 integration dimensions 0.1-0.4** — the code is shipped but none of the integration assertions have been run against a live DB. That needs `make down && make up && make test-integration` (tier 3 gate per AGENTS.md).
- ⏳ **`public/openapi.json` edits** — both the M28_002 §0 rename (`/external-agents` → `/agent-keys`) AND the M29_002 `/spec/template` + `/spec/preview` deletions are blocked waiting for the sibling worktree `feat/m28-webhook-auth-middleware` to merge its OpenAPI-split work to main first. Rebase window comes after that merge.
- ⏳ **`make lint` / `make check-pg-drain` / `make memleak`** — not run this session (no server-lifecycle or allocator-wiring changes beyond handler rename). Should be run before PR #222 lands.
- ⏳ **Cross-compile final gate** (`zig build -Dtarget=x86_64-linux && zig build -Dtarget=aarch64-linux`) — not run this session. Branch touched `*.zig` so this is required per RULE XCC before the PR lands.
- ⏳ **Ripley's Log for this session** — written alongside this handoff (`LOG_APR_18_17_23_28_M28_002.md`).

---

## Working Tree

```
## feat/m28-api-keys...origin/feat/m28-api-keys
(in sync — nothing to push)
```

**Commits on branch this session** (added on top of the prior session's 11 commits):

```
c7444054 spec: add M29_001 doc rewrite + M29_002 remove v1 spec/run flow (pending)
b41596c1 chore: rename m<milestone>_ test files to purpose-based names
c2716e07 feat(m28_002): rename external_agents -> agent_keys across src + cli (steps 2+3)
b2f28d5d feat(m28_002): rename schema 027 external_agents -> agent_keys (step 1)
3383bfba docs(nostromo): handoff — M28_002 spec complete, EXECUTE not started   [prior session]
abb102c0 spec: add P2_INFRA_M28_003 openapi split, bundle, and sync gate (pending)
a8ad7c0d spec(m28_002): add adversarial brute-force regeneration test for key entropy
e5429506 spec(m28_002): complete /write-unit-test tier coverage + key entropy tests
f78a2d3c spec(m28_002): close 10/10 quality gaps — pagination, PATCH contracts, obs, DDL
1cd3d5d4 spec(m28_002): close /review gaps — openapi row, step 9, tenant iso test
2089e04b spec(m28_002): adopt REST guidelines — PATCH for revoke, reference design docs
a189dc0f spec(m28): rename workstream 001 → 002; address greptile review
c2bb6abc chore: remove pending copy after move to active/
ae487bf9 spec: add §0 rename external_agents → agent_keys, update all references
142790a6 chore(open): move M28_001 spec to active, status=IN_PROGRESS
b1f4745f spec: add P1_API_CLI_M28_001 tenant API key management (pending)   [merge base]
```

All commits are pushed to `origin/feat/m28-api-keys`. Working tree clean (after the handoff + log commit).

**Key files on branch:**

- `docs/v2/active/P1_API_M28_002_TENANT_API_KEY_MANAGEMENT.md` — 608 lines, `Status: IN_PROGRESS`. §0 now annotated IN_PROGRESS with Dim 0.5 DONE; §1-§5 all PENDING.
- `docs/v2/pending/P2_INFRA_M28_003_OPENAPI_TOOLING.md` — 394 lines, PENDING (carried from prior session).
- `docs/v2/pending/P0_CLI_M29_001_DOC_REWRITE_SPEC_TO_ZOMBIE.md` — 378 lines, PENDING (new this session).
- `docs/v2/pending/P1_API_CLI_M29_002_REMOVE_V1_SPEC_RUN_FLOW.md` — 410 lines, PENDING (new this session).
- `schema/027_core_agent_keys.sql` — the renamed workspace-agent-key table (replaces the deleted `027_core_external_agents.sql`).
- `src/http/handlers/agent_keys.zig` — renamed from `external_agents.zig`. Handler functions now `innerCreateAgentKey`, `innerListAgentKeys`, `innerDeleteAgentKey`. Log scope `.agent_keys`. Log events `agent_key.created` / `agent_key.deleted`.

**Dotfiles state:**

- Repo: `git@github.com:indykish/dotfiles`, branch `master`, pushed as `841c340`.
- Backup of pre-edit `AGENTS.md`: `/Users/kishore/Projects/dotfiles/AGENTS.md.bak_20260418_164548`. Safe to delete once the milestone-ref scrub has been reviewed in context.
- The hardlinked `docs/greptile-learnings/RULES.md` (shared between dotfiles and usezombie via inode) got the RULE FLL extension. The usezombie repo does not track this file (earlier symlink-conversion commit `e6013e0b` dropped it from usezombie's index).

**Sibling worktrees (not touched this session):**

- `/Users/kishore/Projects/usezombie-m28-webhook-auth` (`feat/m28-webhook-auth-middleware`) — another agent owns this; M28_001 webhook-auth middleware + OPENAPI_SPLIT spec. **Expected to merge to main first**, then this branch rebases for `openapi.json` edits.
- `/Users/kishore/Projects/usezombie-m11-invite-signup`, `/Users/kishore/Projects/usezombie-m12-app-dashboard` — older branches, untouched.

---

## Branch / PR

- **Forge:** GitHub
- **Repo:** `usezombie/usezombie`
- **Branch:** `feat/m28-api-keys`
- **PR:** #222 — `spec: M28_002 Tenant API Key Management`
- **State:** OPEN, BLOCKED on reviewer (branch protection). Not a CI failure.
- **CI** (from prior session, unchanged this session because no code change triggered path-filtered smoke tests to run against spec-only commits from earlier; §0 rename commits DID touch `src/**` but CI state post-`c7444054` not re-checked in this session):
  - `Analyze (javascript-typescript)` — SUCCESS historically
  - `gitleaks` — SUCCESS (ran via pre-commit hook locally on every commit this session)
  - `smoke` (×3) — SKIPPED before §0 rename landed; should have re-run after `b2f28d5d`/`c2716e07`/`b41596c1`. Not verified in this session.
  - Cross-compile / `make test` / `make test-integration` — not applicable at spec level; **should be verified at branch level before PR lands.** See "Next Steps" item 3.

**PR is now significantly heavier** than when the prior session closed. In addition to the 602-line M28_002 spec and the 394-line M28_003 spec, it now carries:

- the §0 rename (schema + embed + migration array + handler + routes + matchers + auth doc + zombiectl URL)
- the stale handoff deletion
- the 10-file test-file rename pass
- the two new M29 pending specs (778 lines)

Reviewer should expect ~1100 spec lines + ~150 code lines + 10 renames. If the reviewer wants the PR split, the cleanest split boundaries are:
1. Spec-only commits (everything through `abb102c0`).
2. §0 rename (`b2f28d5d` + `c2716e07`).
3. Test file renames (`b41596c1`).
4. M29 pending specs (`c7444054`).

Each is independently landable if the reviewer prefers that.

---

## Running processes

None. No tmux sessions, no dev servers, no test watchers, no background jobs. `tmux ls` reports "no server running".

---

## Tests / Checks run this session

- `zig build` — PASS (after Step 1 schema rename)
- `zig build` — PASS (after Step 2 handler + route rename)
- `zig build test` — PASS (after Step 2; exit 0, no unit-test failures)
- `zig build` — PASS (after test-file renames)
- `zig build test` — PASS (after test-file renames; exit 0, same benign log warnings as before)
- `make test` — NOT RUN this session (the Makefile wrapper; `zig build test` was the direct invocation)
- `make test-integration` — NOT RUN (would cover §0 Dims 0.1-0.4)
- `make lint` — NOT RUN
- `make check-pg-drain` — NOT RUN (but no new `conn.query()` call sites introduced; only SQL string literals changed table name)
- `make memleak` — NOT RUN (no server-lifecycle or allocator-wiring changes)
- Cross-compile `x86_64-linux` / `aarch64-linux` — NOT RUN
- `gitleaks detect` — RAN via pre-commit hook on every commit this session, all PASSED

---

## Next Steps (ordered for the next agent)

1. **Decide PR #222 strategy.** Either (a) execute M28_002 Steps 4-13 on this branch and ship the full tenant-API-key feature in PR #222 (biggest PR, longest to land), or (b) ship PR #222 as-is (spec + §0 rename + test renames + M29 pending specs) and open a sibling worktree `feat/m28-api-keys-handlers` for Steps 4-13. Option (b) is cleaner for review but requires the spec to move from `active/` back to `pending/` or stay in `active/` while work happens on a sibling branch (the lifecycle rules don't handle this cleanly — needs a human decision).
2. **Run the branch-level hygiene gates** before declaring PR #222 ready to land (independent of the scope decision in item 1):
   - `zig build -Dtarget=x86_64-linux && zig build -Dtarget=aarch64-linux` — RULE XCC.
   - `make lint` — hard gate.
   - `make check-pg-drain` — SQL string literals were edited, even though `conn.query()` call sites were not structurally changed; belt-and-suspenders.
   - `make test-integration` — especially to prove §0 Dims 0.1-0.4 against a live DB. Expect a fresh DB (`make down && make up`) to catch any migration version-number or embed-file-rename issues.
   - `gitleaks detect` — already run on every commit via pre-commit hook, but final branch-level sanity sweep.
3. **After sibling `feat/m28-webhook-auth-middleware` merges to main**, rebase `feat/m28-api-keys` onto the new main and do the deferred `public/openapi.json` edits in a single commit:
   - Rename `/v1/workspaces/{workspace_id}/external-agents` → `/agent-keys` (M28_002 §0 tail). Four path entries in `public/openapi.json:2308, 2473` and the `operationId` strings `list_external_agents` and `delete_external_agent`.
   - Defer the `/spec/template` + `/spec/preview` deletions — those are M29_002 scope, not M28_002.
4. **If executing Steps 4-13 on this branch**: per the spec's Execution Plan, Step 4 is "Schema: create `030_api_keys.sql`, update `embed.zig` + `common.zig` migration array". Before the first file edit, fire the **Schema Table Removal Guard** even though this is an ADD (the guard fires on any `schema/*.sql` edit per AGENTS.md). `cat VERSION` reads `0.18.0` — pre-v2.0, full teardown branch applies — but since this is a CREATE not a DROP, the guard output simply documents the new file + embed + migration-array entries being added.
5. **Start `/write-unit-test` on each dimension** once Steps 4-13 produce code. The spec's Tier Coverage Matrix at the top of `§Test Specification` is the acceptance contract — every tier (Unit, Integration, Negative, Edge Case, Regression, Leak, Concurrency, Security, Fidelity, Constants, Performance, API Contract) must have at least one row in the produced test suite.
6. **Do NOT start M29_001 or M29_002 from this worktree.** Both are `Status: PENDING` in `docs/v2/pending/`; when work begins, each gets its own worktree off `main` with a fresh feature branch and spec moves to `active/`. Parallel batches B1 per their spec headers.

---

## Risks / Gotchas

- **PR #222 blast radius has grown significantly.** When the prior handoff was written, the PR was spec-only. It is now spec + 150 lines of Zig/SQL/JS code + 10 test-file renames + 778 lines of two new pending specs. Reviewer sign-off may be harder to secure on a single PR of this size. See PR strategy item 1 above.
- **§0 integration dimensions (0.1-0.4) have NOT been verified against a live DB.** The code compiles and unit tests pass, but nobody has run `make test-integration` to prove the DDL renames land correctly on a fresh DB, that `POST /v1/workspaces/{ws}/agent-keys` actually returns 201, or that `DELETE` actually removes the row. This is the standard pre-v2.0 tier-3 risk: Unit tests passing does not mean the migration version number is honored when the DB is wiped and rebuilt. The migration array still lists version 27 under a new variable name (`core_agent_keys_sql`) — in theory the migration runner treats this as the same version it already applied, so a DB upgraded past version 27 will silently continue to host a table named `core.external_agents`. **Only a teardown-rebuild (`make down && make up && make test-integration`) proves §0 is correct.** Flag this to the reviewer.
- **`agent_keys.zig:1` header and `router.zig:69` enum comment still reference `external_agents`** — this is intentional archaeology (same pattern as `schema/027_core_agent_keys.sql:1,7`) but future greppers may flag them. The orphan-sweep output was honest about this.
- **Two archaeology comments in schema** (`schema/004_vault_schema.sql:40` and `schema/020_...sql:1,77`) reference the long-deleted `core.specs` / `core.runs` tables. M29_002's Dead Code Sweep explicitly exempts these. Do not touch them during M29_002 execution.
- **M29_002 deletes the `SPEC_TEMPLATE_SYSTEM_PROMPT` + `SPEC_PREVIEW_SYSTEM_PROMPT` constants** inside `src/http/handlers/agent_relay.zig` but keeps the handler file (user direction). The M29_002 spec reflects this. Whoever executes M29_002 must NOT delete `agent_relay.zig` — the generic LLM relay primitive stays; only the two spec-specific prompts go.
- **Brute-force regeneration test** and **entropy tests** (100k/1M depending on optimize mode) live inside `make test` per user direction. Budget ~5-10s in ReleaseFast, ~15-30s in Debug when the tests are actually implemented in Step 10. Do not add a separate `make test-entropy` target.
- **`last_used_at` stamping is DEFERRED to a Redis-queue + zombie-worker reaper workstream** that has not been specced yet. When Step 10 (integration tests) runs, the `GET /v1/api-keys` response body will include `last_used_at: null` for every row until that deferred workstream ships. This is documented in the spec's Out of Scope and cross-referenced in five row annotations.
- **`zombiectl/src/commands/agent_external.js` file and subcommand name were NOT renamed** this session — only the URL was updated to `/agent-keys`. File and subcommand rename is deferred to a future CLI milestone (the spec explicitly defers zombiectl to a CLI milestone).
- **Terminology rule is self-enforcing now.** `AGENTS.md` and the M28_002 spec itself use placeholder forms (`M{N}`, `M{N}_{WS}`) instead of literal milestone refs in durable prose. If Step 4+ implementation adds literal milestone refs to new `.md` specs (e.g. "M28_002 postmortem"), they violate the rule. Keep the placeholder form.
- **RULE FLL now has a folder-file-count clause (≤15 non-test source files per directory, recursive).** `src/http/handlers/` is currently close to the cap; adding `api_keys.zig` (Step 8) may push it over. Count before adding and propose a subdirectory split if needed.
- **`docs/v2/pending/` directory count.** There are 12 pending specs now (10 pre-existing + 2 added this session). This doesn't affect RULE FLL (only applies to `.zig`/`.js`/`.ts`) but is worth noting for branch-readiness — the pending dir is getting dense.
- **Dotfiles and usezombie `docs/greptile-learnings/RULES.md` are hardlinked to the same inode.** Editing one updates both. Only dotfiles tracks the file; usezombie stopped tracking it at commit `e6013e0b`. If a future session edits RULES.md thinking it's just a local change, the dotfiles repo will see the change and need a commit.
- **The stale handoff (`HANDOFF_APR_16_01_29_03.md`) was deleted under user direction.** Future sessions should refer to this handoff (`HANDOFF_APR_18_17_23_M28_002.md`) and the Ripley's log (`LOG_APR_18_17_23_28_M28_002.md`) for the current branch state.
