# Handoff — M28_002 Tenant API Key Management (spec phase complete, EXECUTE not started)

**Date:** Apr 18, 2026: 09:22 AM UTC
**Worktree:** `/Users/kishore/Projects/usezombie-m28-api-keys`
**Branch:** `feat/m28-api-keys`
**PR:** [#222](https://github.com/usezombie/usezombie/pull/222) — `spec: M28_002 Tenant API Key Management` — OPEN, state BLOCKED (branch protection; needs reviewer)

---

## Scope / Status

This session took the M28_002 spec from "greptile-flagged draft" to "10/10 implementation-ready spec" and opened a follow-on workstream (M28_003) for OpenAPI tooling. **No code was written** — the whole branch is spec edits.

- ✅ **Renamed M28_001 → M28_002.** Another agent owns M28_001 on `feat/m28-webhook-auth-middleware` for unified webhook auth. PR title and spec contents updated.
- ✅ **Greptile review addressed.** All five P1/P2 comments resolved in the spec (key-length inconsistency, 404-vs-409 for already-revoked, DELETE-verb semantics, last_used_at latency, tenant isolation).
- ✅ **Adopted REST guidelines.** Revoke is now `PATCH /v1/api-keys/{id}` with `{"active": false}`, not `POST /…/revoke` — conforms to `docs/REST_API_DESIGN_GUIDELINES.md §7` (no verbs in URLs).
- ✅ **Authoritative References block** added pinning the spec to `REST_API_DESIGN_GUIDELINES.md`, `api_handler_guide.md`, and `M11_002_HX_HANDLER_CONTEXT.md`.
- ✅ **/review pass.** Three informational findings auto-fixed (openapi.json in Files Changed, Step 9 verify command, dim 3.7 integration test row).
- ✅ **PR quality score 10/10.** Closed nine gaps: pagination, PATCH body semantics, `last_used_at` mechanism (post-flush deferred hook), DDL inlined with GRANT + CHECK constraint, §5 Observability (full event + metric catalog), filename dropped `_CLI` (rename: `P1_API_CLI_M28_002` → `P1_API_M28_002`).
- ✅ **Full /write-unit-test tier coverage.** Added a Tier Coverage Matrix at the top of Test Specification binding every tier to a row count. New tiers: Concurrency (concurrent CREATE race), Security (SQL injection, raw-key-no-log, timing-safe compare), Fidelity (response shape exact match), Constants (grep gates), Performance (p95 budget), API Contract (openapi ↔ router parity), Leak (deferred-hook path).
- ✅ **Key entropy & crackability tests.** Honest framing + 7 rows: CSPRNG source, Shannon entropy ≥ 7.95, zero collisions at 1M mints, bit-level balance, inter-key Pearson correlation, keyspace bound (2²⁵⁶ comptime assertion), timing-equivalence unknown-vs-revoked. Plus an **adversarial brute-force regeneration test** with full Zig pseudocode — tries to recover a real minted key by seeding `DefaultPrng` / `Xoshiro256` / `Sfc64` with ±1s window timestamps across a 10-iteration loop.
- ✅ **M28_003 spec drafted** — `P2_INFRA_M28_003_OPENAPI_TOOLING.md` in `docs/v2/pending/`. Split + bundle + sync gate approach. Python drift gate matching existing `scripts/check_openapi_errors.py` pattern. Zig-native OpenAPI generation explicitly out of scope (separate future project).
- ✅ **Dotfiles: Terminology rule.** Added "Terminology — forbidden substitutes" section to `AGENTS.md` (symlinked as `~/.claude/CLAUDE.md`). Commit `a8a78e5` already on `origin/master`. Stops agents from drifting to Phase/Sprint/Epic vocabulary.

### What's NOT done

- ⏳ **EXECUTE phase** — zero Zig code written. The spec is the full deliverable for this branch so far.
- ⏳ **M28_003 not on `main` yet.** Per spec lifecycle, pending specs should live on `main` before the workstream is started. Currently only on `feat/m28-api-keys`. Needs cherry-pick or separate PR if someone wants to start M28_003 independently.
- ⏳ **Worktree integration tests, cross-compile, leak gates** — none run. No code to run them against.

---

## Working Tree

```
## feat/m28-api-keys...origin/feat/m28-api-keys
(in sync — nothing to push)
```

**Commits on branch (vs `origin/main`):**

```
abb102c0 spec: add P2_INFRA_M28_003 openapi split, bundle, and sync gate (pending)
a8ad7c0d spec(m28_002): add adversarial brute-force regeneration test for key entropy
e5429506 spec(m28_002): complete /write-unit-test tier coverage + key entropy tests
f78a2d3c spec(m28_002): close 10/10 quality gaps — pagination, PATCH contracts, obs, DDL
1cd3d5d4 spec(m28_002): close /review gaps — openapi row, step 9, tenant iso test
2089e04b spec(m28_002): adopt REST guidelines — PATCH for revoke, reference design docs
a189dc0f spec(m28): rename workstream 001 → 002; address greptile review
c2bb6abc chore: remove pending copy after move to active/
ae487bf9 spec: add §0 rename external_agents → agent_keys, update all references
142790a6 chore(open): move M28_001 spec to active, status=IN_PROGRESS
b1f4745f spec: add P1_API_CLI_M28_001 tenant API key management (pending)
```

All 11 commits are pushed to `origin/feat/m28-api-keys`. Working tree is clean.

**Key files on branch:**

- `docs/v2/active/P1_API_M28_002_TENANT_API_KEY_MANAGEMENT.md` (602 lines, `Status: IN_PROGRESS`)
- `docs/v2/pending/P2_INFRA_M28_003_OPENAPI_TOOLING.md` (394 lines, `Status: PENDING`)

**Sibling worktrees (not touched this session):**

- `/Users/kishore/Projects/usezombie-m28-webhook-auth` (`feat/m28-webhook-auth-middleware`) — another agent owns this; M28_001 webhook auth middleware spec.
- `/Users/kishore/Projects/usezombie-m11-invite-signup`, `/Users/kishore/Projects/usezombie-m12-app-dashboard` — older branches.

---

## Branch / PR

- **Forge:** GitHub
- **Repo:** `usezombie/usezombie`
- **Branch:** `feat/m28-api-keys`
- **PR:** #222 — `spec: M28_002 Tenant API Key Management`
- **Title note:** matches spec filename convention (`M28_002`, not `M28_001`). Retitled this session.
- **State:** OPEN
- **Merge state:** `BLOCKED` — branch protection requires a reviewer. Not a CI failure.
- **CI:**
  - `Analyze (javascript-typescript)` — SUCCESS
  - `gitleaks` — SUCCESS
  - `smoke` (×3) — SKIPPED (path filters — spec-only changes don't touch app code)
  - Cross-compile / `make test` / `make test-integration` — not applicable (no code changes)

---

## Dotfiles state

- **Repo:** `git@github.com:indykish/dotfiles`
- **Branch:** `master`
- **Commits landed since session start** (note: a concurrent session added `5fccdc4` and `4e81be9` on top of the terminology commit):
  - `4e81be9` — docs(agents): drop parked GPA bullet from memleak hygiene block
  - `5fccdc4` — docs(agents): add Spec Discipline + Memleak gate hygiene (M28_001 postmortem)
  - `a8a78e5` — **agents: add Terminology — forbidden substitutes section** ← this session
- All pushed. `master` is in sync with `origin/master`.
- **Backup:** `~/.claude/CLAUDE.md.bak_20260418_142402` — pre-edit snapshot of `AGENTS.md`. Safe to delete once the terminology change has been reviewed in context.

---

## Running processes

None. No tmux sessions, no dev servers, no test watchers, no background jobs.

```
$ tmux ls
(no server running)
```

---

## Tests / Checks

Nothing ran because nothing has code:

- Unit tests (`make test`) — not applicable yet
- Integration tests (`make test-integration`) — not applicable yet
- Leak tests (`make memleak`) — not applicable yet
- Cross-compile (`zig build -Dtarget=x86_64-linux && zig build -Dtarget=aarch64-linux`) — not applicable yet
- `make lint` — not run; no code-side changes
- `make check-pg-drain` — not run; no Zig changes
- **Greptile** — ran this session via `gh api /repos/.../pulls/222/comments`; all five comments classified and addressed. New Greptile review will fire on the next push.

---

## Next Steps (ordered for the next agent)

1. **Decide whether to land M28_003 spec on `main` separately** (so the openapi tooling workstream can be picked up independently) or keep it bundled in PR #222. The spec lifecycle prefers pending specs to live on `main` before their workstream starts — a small cherry-pick PR is the cleanest option.
2. **Start EXECUTE on M28_002** in this worktree. First step per the Execution Plan (Step 1 of 13):
   - Rename: delete `schema/027_core_external_agents.sql`, create `schema/027_core_agent_keys.sql` with table `core.agent_keys`, update `schema/embed.zig` and `src/cmd/common.zig` migration array.
   - Verify: `zig build` compiles.
3. **Invoke the `Schema Table Removal Guard`** before the Step 1 edit (see `AGENTS.md`). `cat VERSION` currently reads `0.18.0` — full teardown branch applies (delete file, remove embed entry, remove migration array entry; never DROP/ALTER pre-v2.0).
4. Run `/write-unit-test` with the spec as input once the first code lands. The Tier Coverage Matrix (top of `§Test Specification`) is the acceptance contract — every tier MUST have at least one row in the produced test suite.
5. After the first commit touches Zig, verify the `make lint` umbrella and `make check-pg-drain` still pass — those are hard gates from M11_001 and earlier.

---

## Risks / Gotchas

- **Two specs, one PR.** PR #222 ships both `active/P1_API_M28_002_...md` and `pending/P2_INFRA_M28_003_...md`. If the reviewer only expects M28_002 content, flag the M28_003 pending spec in the review so they don't miss it. Alternative: split M28_003 into its own PR targeting `main` first.
- **File was renamed twice.** `P1_API_CLI_M28_001_...` → `P1_API_CLI_M28_002_...` → `P1_API_M28_002_...`. Anyone grepping for the old filename will not find it. Git history traces cleanly via `git log --follow`.
- **`public/openapi.json` is 4.3K hand-edited lines.** M28_002 will add ~200 lines for the four api-keys endpoints. Be careful the manual edit lands in the right alphabetical slot and reuses `ErrorBody` via `$ref` per the existing `check-openapi-errors` gate — otherwise CI fails on the bundled lint rules. M28_003 will eventually eliminate this pain; for M28_002, just be surgical.
- **Another agent owns M28_001.** Do not edit anything in `/Users/kishore/Projects/usezombie-m28-webhook-auth` or `feat/m28-webhook-auth-middleware` from this worktree. Merge conflicts in `src/auth/middleware/mod.zig` and `src/http/router.zig` are likely when both branches land — watch for them.
- **`last_used_at` async mechanism is pinned to a specific implementation.** The spec says the stamping runs in a post-flush deferred hook in `cmd/serve.zig`, not a detached thread. The `httpz` response lifecycle will dictate how this hook is actually registered — may require a small shim. Do not silently swap this for a thread-based mechanism without updating the spec + failure modes row.
- **CSPRNG test requires a specific Zig idiom.** The test asserts `generateTenantApiKey` reaches `std.crypto.random.bytes(&buf)`. If the implementer uses `std.crypto.random.Random.bytes(random, &buf)` via an injectable interface (for testability), the test needs to assert the default random source *is* the OS CSPRNG, not that the call site is hardcoded to `std.crypto.random`. Flex the test to the actual architecture.
- **Brute-force regeneration test is CI-gated.** The 1M-key collision test and the brute-force loop are expensive. Gate them behind `make test-entropy` (not `make test`). Do not run them on every `zig build test` or local dev loops will grind.
- **Terminology rule is live.** `AGENTS.md` now forbids "Phase 1/2/3", "Sprint", "Epic", "Story", "Ticket" in durable artifacts. If a grep gate gets added later (proposed but not built), existing done/ specs are exempt — do not retroactively rewrite them.
- **PR is BLOCKED on reviewer, not CI.** Reviewer needs to look at a 600-line spec + a 400-line pending spec. If they want them split, be ready to do the split cleanly (spec file + related commits go together).
