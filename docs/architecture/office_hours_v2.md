# Design: Platform-Ops Wedge — `/usezombie-install-platform-ops` Skill + Architecture Refresh

Generated by /office-hours on Apr 25, 2026
Branch: main
Repo: usezombie/usezombie
Status: SUPERSEDED IN PLACE — see "Revisions since publication" below
Mode: Startup

---

## Revisions since publication (Apr 25, 2026, post-/plan-ceo-review)

This doc is the original /office-hours session output. The strategic narrative below (problem, demand evidence, status quo, persona, premises) stands. The following decisions were made AFTER publication and are reflected in surgical edits throughout the body:

1. **Skill renamed** from `/install-platform-ops` to `/usezombie-install-platform-ops` (same name in every host — no per-host fork). Decision: /plan-ceo-review.
2. **Self-host deferred to v3.** v2 ships hosted-only on `api.usezombie.com` via Clerk OAuth. The `/self-host` runbook is removed from v2 docs scope. Differentiation pillars compressed from 4 to 3 (OSS + BYOK + markdown-defined). Self-host validation specs (the 4 untested rows) moved out of v2.
3. **M48 BYOK promoted to substrate-tier.** Was "soft-blocks BYOK launch claim"; now Week 2-3 launch-blocking. Adds ~1 week to the milestone (~6-7 weeks total). Differentiation argument holds at 3 pillars.
4. **M50 reassigned.** Originally "README.md cross-reference + post-launch reflection" — folded into M51. Slot reused for "M50 Customer-Development Parallel Workstream" — runs Weeks 1-8 alongside substrate, ~2-3 hours/week founder time, goal: 3 named operators committed to Day-35 install slots.
5. **Customer-dev runs in parallel from Week 1**, not sequentially after Week 6. Eliminates the "ship into silence" risk; validates wedge framing while substrate is mutable.

The Week-by-Week sequencing in §"Recommended Approach" below is now ~6-7 weeks instead of 5-6. Day-50 validation bar unchanged.

For the canonical, current spec backlog see `docs/v2/pending/M40_001`–`M51_001`. The tier breakdown (substrate / packaging / parallel-validation / post-launch) lives in `docs/v2/pending/M48_001_P1_API_CLI_UI_BYOK_PROVIDER.md` under "Tier promotion provenance" — folded there because M48's substrate-tier promotion is the load-bearing reason the table exists.

---

## Problem Statement

Operational outcomes fall into limbo. When a deploy fails, an inference endpoint starts returning 404, or production looks unhealthy, the SRE manually correlates signals across 5+ tools (Grafana, SigNoz, Zabbix or equivalent, app logs via gRPC observability with tokens in 1Password, GitHub Actions runs, SSH-to-baremetal), holds the timeline in their head, frequently restarts services with no understood root cause, and has no automatic mechanism to keep customers or stakeholders informed during the incident.

For the v2 MVP, the sharpest, datedmost-recent instance of this pain is **deploy and general production-outage triage**. Customer Zero (the author) has a currently-broken usezombie dev CD pipeline ([GH Actions run #24861966717](https://github.com/usezombie/usezombie/actions/runs/24861966717/job/72790030492#logs)) and an SRE workflow at E2E Networks where outage triage is the cursed step. The same pattern recurred at gettune.com (2020s) — AWS Event Queue down, S3 PDF upload failures, customer-facing comms gaps, signal-correlation witch hunts.

The wedge is not "fix all SRE problems." It is: **let any small-team operator install a markdown-defined, durable, BYOK zombie that watches their GitHub Actions CD pipeline, reasons over failures, posts an evidenced diagnosis to Slack, and (later) extends into the bastion role — owning incident communication and customer-facing status.** [REVISED Apr 25: "self-hostable" removed from the v2 claim; self-host arrives in v3.]

## Demand Evidence

Honest demand bucket (per D1): **just my own pain — no external test yet (option D)**. Caveats and adjacent signal:

- Customer Zero has dated, concrete pain right now (broken dev CD migration, real GH Actions URL).
- Past-life recurrence: at gettune.com (CTO, 2020s), the same outage-comms gap caused CEO escalations during AWS S3 / Event Queue outages. Pattern is not novel to E2E.
- Adjacent vendor TAM is real: AWS DevOps Agent went GA Mar 31, 2026; Sonarly (YC W26) shipped; incident.io ships AI SRE; Atlassian Statuspage ships AI auto-drafting (Feb 2026). The category is intensely validated by competitors — the question is differentiation, not whether anyone wants this.
- "Will pay" / "got angry when prototype broke" / "named human" demand: **none yet**. The honest assignment is to convert this to ≥3 named operators within 2 weeks of wedge ship.

## Status Quo (Customer Zero workflow today)

Recent work-outage example: user-facing agent reports DB calls slow → SSH to host → open Grafana → open observability gRPC endpoint (token fetched from 1Password) → switch to browser for Zabbix metrics in time window → check Django API logs → still no clear cause → **restart service with no clue why** (the SRE failure mode). 5+ tools, no shared timeline, no persistent memory across incidents, no automatic stakeholder communication.

The ONE worst step (per D3) is **cross-correlating signals across tools** — bouncing tabs while holding the timeline in one's head at 2am. Adjacent pain explicitly named: **outage communication is manual; customers complain they aren't informed**.

## Target User & Narrowest Wedge

**Persona** (per D4 + D7 sharpening):

- Primary: SRE / platform engineer / 10x indie engineer / small high-agency team running their own infra (OVH bare metal today; AWS VMs / DO droplets / on-prem GPU clusters at the broader category) who **cannot or will not pipe their full ops stack into another SaaS** (cost, security, data sovereignty, SaaS-fatigue).
- Secondary: agentic-OS / agent-economy operators where human-in-the-loop is minimal — small teams that need a durable runtime, not another vendor dashboard.
- Customer Zero: the author at E2E Networks, with a broken dev CD as the immediate dogfood surface.

**Narrowest wedge** (per D5, D8): **Approach B — a SKILL.md invoked as `/usezombie-install-platform-ops`** (same slash-command in every host: Claude Code, Amp, Codex CLI, OpenCode), distributed via the `usezombie/skills` repo and fetchable from `https://usezombie.sh/skills.md`. The skill auto-detects the user's repo (CI provider, deploy target, Slack channel inferred from `.github/workflows/*.yml`, `fly.toml`, `Dockerfile`, etc.), asks 3 tight gating questions, generates a tailored `.usezombie/platform-ops/{SKILL,TRIGGER,README}.md` in the user's repo, helps resolve credentials, and drives `zombiectl install --from .usezombie/platform-ops/` + `zombiectl steer`.

The skill is the install UX. No bash one-liner (Approach A dropped — cross-platform shell-test cost > value when SKILL.md is already portable across multiple agent CLIs).

## Constraints

- Cannot out-spend Sonarly's YC funding or AWS's marketing budget. Differentiation must be structural (OSS / BYOK / markdown-defined; self-host deferred to v3), and distribution must be build-in-public + Customer Zero + open-source GitHub stars.
- M37_001 platform-ops sample (SKILL.md + TRIGGER.md + README.md) already shipped — the skill must consume that, not replace it.
- M19_003 (`zombiectl install --from <path>`) shipped — the skill leans on it.
- ~~"Self-hosted" today is a partially-validated claim~~ [REVISED Apr 25: self-host moved to v3 entirely. v2 ships hosted-only on `api.usezombie.com` via Clerk OAuth. The Postgres/Redis substitutions remain conceptually valid for v3, but no `/self-host` runbook ships in v2.]
- The skill must work without exhausting users with questions — strict ≤4 AskUserQuestion gates total.

## Premises

Accepted in this session (per D7), with the noted sharpenings:

- **P1**: TAM is "operators at teams that cannot/will not pipe their ops stack into another SaaS," explicitly including AI-infra companies, GPU cloud providers, regulated mid-market, agentic-OS/agent-economy operators, 10x indie engineers, and small high-agency teams. NOT "all SREs everywhere."
- **P2**: Differentiation = OSS + BYOK + markdown-defined durable agent runtime. OSS is true today. BYOK ships in v2 substrate (M48 promoted to launch-blocking on Apr 25). [REVISED Apr 25: "self-hostable" pillar removed; self-host deferred to v3.]
- **P3**: Wedge C (GH-Actions-CD-failure responder) is the right first artifact, marketed as "*your* zombie running on *your* infra against *your* CI" — not "another GH-Agentic-Workflows-style add-on."
- **P4**: The bastion → automatic-statuspage extension is real and valuable but competes head-on with Atlassian Statuspage AI (Feb 2026) + Dust + PageCalm. Earn trust on internal-only incidents first; don't lead with customer-facing comms in v1.
- **P5**: Distribution = build-in-public + Customer-Zero dogfood + OSS GitHub stars. The skill itself is a public, screenshot-able artifact. Agents currently spin in OVH baremetal — that's the demo backdrop.

## Approaches Considered

### Approach A: Curl-pipe-bash installer (rejected)
Single shell script piped from GitHub raw URL. Wraps `zombiectl install --from samples/platform-ops` with credential prompts. **Rejected** because cross-platform shell testing (macOS / Linux flavors / WSL) eats founder time, and SKILL.md is already portable across Claude Code, Amp, Codex CLI, and OpenCode without a bash port.

### Approach B: Skill `/usezombie-install-platform-ops` (chosen)
SKILL.md that detects the user's repo, asks 3 tight gating questions, generates tailored `.usezombie/platform-ops/{SKILL,TRIGGER,README}.md`, resolves credentials via `op` / env / interactive, calls `zombiectl install --from .usezombie/platform-ops/`, opens `zombiectl steer`. Effort M (~1 week human / ~2 hours CC). Reuses M19_003 + shipped `samples/platform-ops/`. [REVISED Apr 25: skill renamed from `/install-platform-ops` for brand visibility; same name across all hosts, no plugin manifest, no per-host fork.]

### Approach C: GitHub Action template + hosted demo (deferred)
`samples/.github/workflows/zombie-on-failure.yml` + `demo.usezombie.com`. Pull-based fit to the wedge, but pushes the hosted-vs-self-host decision before the wedge is validated. **Deferred to v2.5** as the second wedge once B has 5 external installs.

## Recommended Approach (revised post-Codex, 5-6 week milestone)

The original design proposed a 1-week packaging milestone. The eng review + Codex outside-voice surfaced that the substrate underneath is incomplete — the install contract is broken, M33 (worker control + chat) is pending, structured creds + network policy aren't wired through. Packaging on top of that substrate produces a Potemkin demo. Revised plan:

### Week 1-2: Substrate (launch-blocking — promote pending specs to active)

1. **Install API contract alignment.** Fix the CLI POST shape `{source_markdown, trigger_markdown}` ↔ API `{name, config_json, source_markdown}` mismatch. Reconcile `tools:` (sample) vs `skills:` (parser). One unit test per shape. (zombiectl/src/commands/zombie.js:83 ↔ src/http/handlers/zombies/api.zig:32 ↔ src/zombie/config_parser.zig:108)
2. **Auth fix + extend `zombiectl doctor`.** Remove `"zombie.install"` from `AUTH_EXEMPT_ROUTES` (zombiectl/src/cli.js:36). **Extend the existing `zombiectl doctor` command** (already auth-exempt, already a diagnostic surface — `routes.js:6`) to verify token validity + server reachability + active workspace + workspace binding. NO new `preflight` sibling command. The install-skill calls `zombiectl doctor --json` before invoking install; on failure it surfaces the doctor output verbatim. Applies to BOTH hosted users (Clerk OAuth token) AND self-host users (local API token). Plus optional cleanup chore: namespace `login`/`logout` under `zombiectl auth login` / `zombiectl auth logout` if not already done.
3. **Promote M33_001 (worker control stream + steer CLI) from `docs/v2/pending/` to `active/`.** Operator-initiated interaction surface. New zombies discoverable by the worker without restart. **CLI command is `zombiectl steer {id} [<message>]`** (NOT `chat` — that name was rejected; steer matches the existing server endpoint `POST /v1/.../zombies/{id}/steer` per `src/http/handlers/zombies/steer_integration_test.zig`). Interactive mode reads stdin, batch mode takes the message inline. M33_001's spec needs amending to use `steer` consistently. This IS the recommended interaction model — decided in office-hours D3 (the bastion framing).
4. **Promote M34_001 (event history + webhook ingestion) from pending to active.** Required for the GH Actions wedge: GH posts on `workflow_run.conclusion=failure` → arrives as synthetic event on `zombie:{id}:events` with `actor=webhook:github`. Same reasoning loop as a chat steer; just a different actor.
5. **Promote M35_001 (per-session policy + credential templating) from pending to active.** Wire structured `{host, api_token}` creds through `createExecution`. Make the executor receive tool config + network policy. Replace `http_request`'s empty `allowed_domains = &.{}`.
6. **Extend M37_001 platform-ops sample with a GH Actions webhook trigger path.** Today's TRIGGER.md says `trigger.type: chat` — extend to `trigger.types: [chat, webhook:github]` (or equivalent dual-trigger schema). Add `webhook.payload_schema` for the GH Actions failure event. SKILL.md prose teaches the agent to reason over GH run logs same way it reasons over fly logs — `http_request` GETs the failed run's logs via GitHub's API. **This is the wedge moment.**
7. **Optional: partial M36_001 (live watch + docs)** if needed for the launch demo's audit trail.

### Week 3-4: End-to-end validation

6. ~~**Self-host validation on clean Ubuntu 24.04**~~ [REVISED Apr 25: removed. Self-host moved to v3 entirely; no v2 validation pass on a vanilla Linux host.]
7. **Customer Zero dogfood**: install platform-ops on the author's repo, run a real chat steer, verify zombie posts to Slack with credentials substituted at the tool-bridge, all evidence in `core.zombie_events`.
8. **[ADDED Apr 25] Customer-dev parallel workstream** (M50 spec): cold-DM 5 named operators/week, demo asset shipped Week 2, weekly Friday Ripley's Log. Goal: by Week 5, ≥3 confirmed install slots booked for the post-ship window.

### Week 5-6: Install-skill + packaging (the original design, on solid substrate)

8. **The install-skill** — published in `usezombie/skills` repo (per A2). Path: `usezombie/skills/usezombie-install-platform-ops/SKILL.md` + an `eval/` directory in `usezombie/skills-evals`. Same `/usezombie-install-platform-ops` invocation in every host (no plugin manifest, no per-host fork).
   - **Frontmatter schema** (per Q1): gstack-conformant top-level + `x-usezombie:` extension block for runtime-specific config (network allowlist, credentials, budget, context knobs).
   - **Multi-agent compatibility** (per A1): host-neutral `variables:` frontmatter + natural-language Q&A. Reference: github.com/resend/resend-cli/tree/main#agent-skills. NOT Claude-Code-specific `AskUserQuestion`.
   - **Repo detection**: read `.github/workflows/*.yml`, `fly.toml`, `Dockerfile`, `pyproject.toml`, `package.json`. Bail on non-GH-CI in v1 (per OQ3).
   - **Install path** (per A4): hosted zombid via Clerk OAuth. [REVISED Apr 25: self-host opt-in branch removed; v2 is hosted-only, no `/self-host` runbook URL.]
   - **Auth + readiness check** (per A9): skill calls `zombiectl doctor --json` first. If unauthenticated or unbound to a workspace, prompts `zombiectl auth login` (which kicks off Clerk OAuth).
   - **Template fetch + cache** (per A5): skill fetches `samples/platform-ops/` from `raw.githubusercontent.com/usezombie/usezombie` at version-pinned tag, caches at `~/.cache/usezombie/skills/usezombie-install-platform-ops/{tag}/`.
   - Generates `.usezombie/platform-ops/{SKILL.md, TRIGGER.md, README.md}` in the user's repo with resolved values substituted. Refuses to overwrite without `--force`.
   - Calls `zombiectl install --from .usezombie/platform-ops/` then `zombiectl steer {id}`.
9. **`usezombie.sh` Cloudflare CDN activation** (per A2) — gates the launch tweet. `https://usezombie.sh/skills.md` resolves; the install-skill is fetchable.
10. ~~**`/self-host` runbook on docs.usezombie.com**~~ [REVISED Apr 25: removed from v2 scope. Self-host arrives in v3. The 4 untested rows are NOT spec'd in v2 pending/.]
11. **`docs/architecture/` refresh** — completed Apr 25, 2026. The current architecture doc is the canonical reference; supersedes all sub-bullets in this item.
12. **`~/Projects/docs/` (docs.usezombie.com source) updates** — owned by M51 spec:
    - Hero copy: *"Durable, BYOK, markdown-defined agent runtime — for operators who own their outcomes."* [REVISED Apr 25: dropped "self-hostable" from hero; sub-line says "Free hosted. Open source. Self-host arrives in v3."]
    - `/quickstart/platform-ops` — install + first-steer walkthrough.
    - `/skills` — describes the `usezombie-*` skill family and the single install procedure (drop the SKILL.md directory into the host's skills folder).
    - `/concepts/context-lifecycle` — user-facing version of architecture §11.
    - `/privacy/cli-telemetry` — anonymous install-pingback contract.
    - ~~`/self-host`~~ [REVISED Apr 25: page intentionally absent in v2; URL returns 404.]
13. **Install-pingback endpoint** — `POST https://api.usezombie.com/v1/skills/install-pingback`. Server-side counter for Day-N install metric. Anonymous, opt-out. Privacy doc per #12.
14. **Wedge framing**: **GH Actions CD-failure responder** — what was decided in office-hours D5 and what the Week 1-2 substrate work specifically enables (M43 webhook ingest + M37 sample extension). Launch tweet leads with "*your zombie watches your GitHub Actions CD pipeline; when a deploy fails it diagnoses with evidence and posts to your Slack.*" Steer is the secondary interaction mode (manual investigation, follow-up steers, "morning health check") — same SKILL.md reasoning loop, different `actor=` on the event.

### Week 7+: Validation (revised bar per Codex tension #4)

- **By Day 50** (~6 weeks post-launch): **1 external team has kept the install ON for a week and gets value from a real production event.** Names + the event details go in the Ripley's Log. NOT "3 installs in 2 weeks" — that measures setup pain tolerance, not demand.
- Concierge installs welcome — author-driven first 1-3 customers is fine. Self-serve installs are a Day-90 metric, not Day-50.
- [ADDED Apr 25] The ≥3 install slots needed for Day-50 are sourced from the **M50 Customer-Dev Parallel Workstream** running Weeks 1-8. By Week 5, the M50 workstream surfaces 3 confirmed slots for the post-ship window. The original Week-7+ "find users" implicit task is now a Week-1 explicit one.

## Cross-Model Perspective (eng-review pass — Codex, Apr 25 2026)

A formal Codex outside-voice pass during /plan-eng-review surfaced findings the office-hours session and my own review missed because they require reading the actual implementation, not the design doc. Findings, with author/Claude response in italics:

- **P0** — `zombiectl install` POST shape doesn't match the API's expected payload (`{name, config_json, source_markdown}` vs `{source_markdown, trigger_markdown}`); shipped sample uses `tools:` while parser requires `skills:`. *Accepted — install contract fix becomes Week 1-2 launch-blocking work.*
- **P0** — README documents liveness + `zombiectl steer` that don't exist yet. Worker discovers zombies once at startup; M33_001 is still pending; `routes.js` has no `chat` or `steer` CLI command (server endpoint exists). *Accepted — M33_001 promoted from pending/ to active/ as Week 1-2 substrate work, with the CLI command landing as `zombiectl steer` (matching the server endpoint name, not the M37_001 spec's earlier `chat` aspiration).*
- **P0** — Sample claims structured creds + per-host network policy + read-only tool boundaries; reality is single `--value` string credentials, no tool/network policy passed to executor, `http_request` built with empty `allowed_domains`. *Accepted — M35_001 (per-session policy + credential templating) promoted as Week 1-2 substrate work.*
- **P1** — Plan claims GH Actions CD-failure responder; shipped M37_001 sample is currently chat-only fly+upstash+slack. *Partially rejected — the wedge stays as decided in office-hours D5: GH Actions CD-failure responder. The chat-only sample is the substrate state today, not the wedge framing. Resolution: promote M34_001 (event history + webhook ingest) to Week 1-2 substrate alongside M33+M35 so the GH Actions webhook can land on `zombie:{id}:events`. Both interactions ship: operator-initiated chat (per M33) AND webhook-triggered events (per M34). Same zombie loop, different `actor=` on the event.*
- **P1** — Context-lifecycle plan uses fields not implemented in code (current checkpoint is just `last_event_id` + truncated `last_response`). *Accepted — only mechanism #1 (rolling tool-result window) ships in v1, since v1's chat-only flow is bounded ~5-10 tool calls. #2 + #3 ride along in M{N+1}.*
- **P1** — `AUTH_EXEMPT_ROUTES` bug is symptomatic; no deterministic preflight check. *Accepted — but no new command. **Extend the existing `zombiectl doctor`** to verify token + reachability + workspace + workspace-binding. Doctor is already auth-exempt and is the natural home for a deterministic readiness check. Plus remove `zombie.install` from `AUTH_EXEMPT_ROUTES`.*
- **P1** — Validation bar of "3 named installs in 2 weeks" measures tolerance for setup pain, not demand. *Accepted — bar changes to "1 external team keeps it ON for a week, gets value from a real event." Day 50 not Day 21.*
- **P2** — Over-engineering packaging before proving the boring job. *Resolved structurally by the new 4-6 week sequencing — Week 1-2 is the boring job, packaging is Week 5-6.*

Net effect: milestone scope ~4× the original design-doc estimate. Launch is delayed from ~1 week to ~5-6 weeks. The launch is real.

## Open Questions

- **OQ1**: Where does the published skill live? In `usezombie/usezombie/samples/platform-ops/.claude/skills/` (single repo, easy to maintain) or in a separate `usezombie/skills` repo (cleaner discovery for `~/.claude/skills/` users)? Decide before Day 1.
Answer: Publish is any global skills registry if we know of. 
Also accessible via https://usezombie.sh/skills.md (this is for agents to access usezombie)

- **OQ2**: Does the skill assume the user already has `zombied-api` reachable, or does it provision one? For v1, **assume reachable** — the skill aborts with a clear message if `zombiectl info` fails. Provisioning a zombied-api in self-host config is a separate workstream (the self-host runbook).
- **OQ3**: For users with no `.github/workflows/*.yml` (non-GH CI: GitLab, CircleCI, Jenkins), does the skill bail or fall back to a generic platform-ops without CD-failure trigger? **Recommendation**: bail in v1 with "GitHub Actions detection required — non-GH CI support is in a future version." Honest scope.
Answer: Yes GH is first then others.
- **OQ4**: Does the skill commit `.usezombie/platform-ops/` to the user's repo, or `.gitignore` it? **Recommendation**: commit it — the SKILL.md/TRIGGER.md ARE the configuration, version-controlled by design. README explicitly tells the user this.

## Success Criteria (revised post-Codex)

- **Day 28-35** (Week 5-6 ship): the install-skill installs platform-ops on Customer Zero's repo, the runtime substrate is real (M33 + M35 + install-contract fixes shipped), the zombie posts an evidenced diagnosis to Slack with credentials substituted at the tool-bridge, every event lands in `core.zombie_events`. Public demo screenshot ships in a docs.usezombie.com release post + tweet.
- **Day 50** (validation, revised bar): **1 external team has kept the install ON for a week and gets value from a real production event** (chat steer → diagnosis → action). Concierge installs OK. Names + event in Ripley's Log. This bar replaces the original "3 installs in 2 weeks" — Codex was right that install count measures setup-pain-tolerance, not demand. Retention + real-event-value are the metrics.
- **Day 90**: 1+ self-serve external install (someone the author didn't help). Day-90 retention ≥ Day-50 retention (no-one churns from "tried it for a week" to "off"). GitHub stars are a context metric, not a target.
- **Failure mode**: if Day 50 produces zero external teams using it on a real event, the wedge framing was wrong. Pivot to operator-discovery (paid customer development) before more code.

## Distribution Plan

- **Skill artifact**: a SKILL.md file in the repo. Distribution = (a) link in README quickstart, (b) `~/.claude/skills/` drop-in instructions, (c) compatibility note for Amp / Codex CLI / OpenCode users.
- **Public demo**: blog post on docs.usezombie.com showing the install on author's repo + the resulting Slack diagnosis. Crosspost as Launch HN if the wedge survives 1 week of dogfood.
- **CI/CD for the skill itself**: it's a markdown file. Lint = `markdownlint`. Test = an integration test that runs the skill against a fixture repo (`samples/fixtures/platform-ops-target/`) and asserts the generated `.usezombie/platform-ops/SKILL.md` contains expected substitutions. Runs in the existing GH Actions test suite.
- **Skill versioning**: the SKILL.md carries a `version:` field. Bump on schema changes. Document substitution variables in a `# Variables` section so users on Amp / Codex / OpenCode can adapt if the AskUserQuestion tool isn't supported.

## Dependencies

- M19_003 (`zombiectl install --from <path>`) — shipped.
- M37_001 (platform-ops sample SKILL.md / TRIGGER.md / README.md) — shipped.
- ~~M21_001 BYOK provider — pending. Soft dependency.~~ [REVISED Apr 25: BYOK promoted to **M48 substrate-tier** (Week 2-3, launch-blocking). The "soft dependency" framing was wrong — with self-host gone, BYOK is one of three differentiation pillars and ships in v2.]
- ~~Self-host validation (the 4 untested rows)~~ [REVISED Apr 25: removed entirely. Self-host arrives in v3; no v2 pending specs for the 4 rows.]
- [ADDED Apr 25] **M50 Customer-Dev Parallel Workstream** — Weeks 1-8, founder-led, ~2-3 hours/week. Goal: 3 named install slots by Day 35; 1 of 3 converts by Day 50.

## The Assignment (revised post-Codex sequencing)

**This week (by Fri May 2, 2026):** open `feat/m{next}-substrate` worktree. Move M33_001 + M34_001 + M35_001 from `docs/v2/pending/` to `active/` (amend M33_001's spec to use `steer` not `chat` for the CLI command name). File new spec `M{N}_001_INSTALL_CONTRACT_AND_DOCTOR_EXTENSION.md` covering the install API contract fix + parser tools↔skills reconciliation + `AUTH_EXEMPT_ROUTES` removal + extending `zombiectl doctor` to verify token + reachability + workspace + workspace-binding (no new sibling command). Optional cleanup: namespace `login`/`logout` under `zombiectl auth login` / `zombiectl auth logout` if not already done. CHORE(open) committed.

**By Fri May 16, 2026 (Week 1-2):** substrate workstreams shipped. Customer Zero can install platform-ops on his own repo via `zombiectl install --from samples/platform-ops/`, run `zombiectl steer {id} "morning health check"`, the worker picks up the new zombie without restart, the steer message hits the executor with full tool config + network policy (M35), the executor's `http_request` actually substitutes credentials at the sandbox boundary, GH Actions failure webhook arrives as a synthetic event on `zombie:{id}:events` with `actor=webhook:github` (M34), and `zombiectl doctor --json` returns clean before any of the above is attempted.

**By Fri May 23, 2026 (Week 3-4):** Customer Zero dogfood of one full chat-steer → fly+upstash query → Slack post cycle works. M50 Customer-Dev workstream has ≥10 operators in active conversation status. [REVISED Apr 25: clean Ubuntu 24.04 self-host validation removed; self-host is v3.]

**By Fri Jun 6, 2026 (Week 5-6):** install-skill ships. Launch demo screenshot ships. usezombie.sh CDN live. Docs updated. Anonymous install pingback live.

**By Fri Jul 4, 2026 (Day 50):** **name and document 1 external team using the install on a real production event for ≥1 week.** Concierge OK. Ripley's Log captures the team, their stack, and what value they reported. This is the validation bar that matters. If you can't name 1, the wedge needs a different shape and the next milestone is operator-discovery (cold DMs to ~15 named SREs at AI infra startups, GPU clouds, regulated mid-market — not "people in your Twitter feed").

## What I noticed about how you think

- **You're rigorous about scope under pressure.** When I pushed on D2 ("which pain woke you up most recently"), you didn't reach for the most strategic answer — you said *"deploy/production outage, since I do SRE."* When you mentioned storage as a real outage, you immediately added *"i don't specifically wanna build for this since the target market for storage persay is small."* Founders who say no to their own pain in service of a sharper wedge are rare.
- **You see the second-order play before the first one ships.** The "bastion / automatic statuspage replacement" framing — *"since the statuspage is very manual with no update any where automatically. Customers usually even today in E2E Networks complain"* — is a real wedge into recurring SaaS, dated to specific 2020 incidents at gettune. You named the pattern across two different companies. That's pattern recognition, not theory.
- **You're honest about your demand bucket.** When I asked for 3 named users, you said *"Honest answer: is D"* and then immediately constructively expanded the persona with the agentic-OS / indie-engineer framing. Founders who lie to themselves at this question lose 3 months. You spent 30 seconds.
- **You think in real stacks, not abstractions.** *"SSH, Grafana, observability gRPC with a token in 1Password, browser for Zabbix metrics, then Django API logs, sometimes I end up restarting with no clue."* — that's the workflow of someone who actually does the job. The product designs you write will be grounded.
