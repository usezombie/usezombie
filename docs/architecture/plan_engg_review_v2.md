# Test Plan
Generated by /plan-eng-review on Apr 25, 2026
Branch: main
Repo: usezombie/usezombie
Status: SUPERSEDED IN PLACE — see "Revisions since publication" below

This file is consumed by /qa and /qa-only as primary test input.

---

## Revisions since publication (Apr 25, 2026, post-/plan-ceo-review)

This doc is the original /plan-eng-review session output. The strategic test framing (skill-flow happy path, repo detection, credential resolution, file generation, install/chat edge cases) stands. The following decisions were made AFTER publication and are reflected in surgical edits throughout the body:

1. **Skill renamed** from `/install-platform-ops` to `/usezombie-install-platform-ops`. Same name in every host (Claude Code, Amp, Codex CLI, OpenCode); no per-host fork.
2. **Self-host deferred to v3.** The "Self-host opt-in path" interaction is removed from v2 scope. Critical Path #3 (cold install + first chat in self-host mode on clean Ubuntu 24.04) is removed. The `/self-host` page is intentionally absent from `docs.usezombie.com`.
3. **`docs.usezombie.com/self-host` page** — removed from "Affected Pages/Routes." `/self-host` returns 404 in v2.

For the canonical, current spec backlog see `docs/v2/pending/M40_001`–`M51_001`. The tier breakdown (substrate / packaging / parallel-validation / post-launch) lives in `docs/v2/pending/M48_001_P1_API_CLI_UI_BYOK_PROVIDER.md` under "Tier promotion provenance" — folded there because M48's substrate-tier promotion is the load-bearing reason the table exists.

---

## Affected Pages/Routes

The wedge does not introduce new HTTP routes — it exercises existing ones. Pages and CLI surfaces a tester needs to verify:

- **CLI surface**: `zombiectl install --from <path>` (existing, regression-test only)
- **CLI surface**: `zombiectl steer {id}` (NEW in M40 substrate work — integration test must verify route lands)
- **CLI surface**: `zombiectl credential add fly|upstash|slack|github|<byok-provider>` (existing for first 3; M48 BYOK adds provider credential resolution)
- **New skill**: `/usezombie-install-platform-ops` (host-neutral SKILL.md in `usezombie/skills` repo; same invocation in Claude Code / Amp / Codex CLI / OpenCode)
- **New page**: `docs.usezombie.com/quickstart/platform-ops`
- ~~**New page**: `docs.usezombie.com/self-host`~~ [REVISED Apr 25: removed. Self-host arrives in v3; the `/self-host` URL intentionally returns 404 in v2.]
- **New page**: `docs.usezombie.com/skills`
- **New CDN endpoint**: `https://usezombie.sh/skills.md`

## Key Interactions to Verify

### Skill flow (any supported host — same in Claude Code, Amp, Codex CLI, OpenCode)

1. User runs `/usezombie-install-platform-ops` in a target repo.
2. Skill detects `.github/workflows/*.yml`, `fly.toml`, `Dockerfile` → infers deploy target.
3. Skill prompts for `slack_channel`, `prod_branch_glob`, `cron_opt_in` (3 variables per M49 B6; BYOK is configured later via `zombiectl provider set` per M48). `deploy_target` may be inferred or confirmed.
4. Skill resolves credentials in order: 1Password CLI (`op read`) → env vars → interactive prompt.
5. Skill writes `.usezombie/platform-ops/{SKILL,TRIGGER,README}.md` with substituted variables.
6. Skill calls `zombiectl install --from .usezombie/platform-ops/`.
7. Skill calls `zombiectl steer {id}` and opens an interactive session.
8. User types "morning health check" → zombie posts evidenced diagnosis to the configured Slack channel.

[REVISED Apr 25: same flow in every host — no per-host fork. The `variables:` frontmatter resolves through host-neutral natural-language Q&A regardless of whether `AskUserQuestion`-style primitives exist.]

### Hosted path (default and only path in v2)

When `zombiectl info` returns no reachable local zombied-api, the skill defaults to the hosted endpoint (`api.usezombie.com`), prompts user to sign in via Clerk if not authenticated, and proceeds. [REVISED Apr 25: this is now the only path. No self-host opt-in.]

~~### Self-host opt-in path (per A3, A4)~~

[REVISED Apr 25: removed. Self-host arrives in v3. The skill does not present a self-host branch in v2; the runbook URL is not printed; no Ubuntu 24.04 VM bring-up procedure ships.]

## Edge Cases

### Repo-detection edge cases

- Repo has no `.github/workflows/` → skill bails with "GitHub Actions detection required — non-GH CI is in a future version" (per OQ3).
- Repo has multiple `*.yml` workflows with different deploy targets → skill prompts user to pick one.
- Repo has `fly.toml` but no `.github/workflows/` → still bails (CI detection is the gate, not deploy target).
- Monorepo with multiple `package.json` files at different paths → skill picks the deepest one matching a workflow path.

### Credential resolution edge cases

- 1Password CLI installed but user not signed in → skill prints `op signin` instructions, retries.
- Env var present but empty → skill ignores and falls through to interactive.
- Interactive prompt receives empty input twice → skill aborts with clear message.
- Token shape mismatches expected format (e.g., user pastes Fly read-write token instead of read-only) → skill cannot validate but prints a warning that token scope matters per the README.

### File-generation edge cases

- `.usezombie/platform-ops/` already exists → skill refuses without `--force`.
- Target repo is a worktree → skill writes to the worktree path, not the main checkout.
- Generated SKILL.md contains a substitution that looks token-shaped → **gitleaks scan blocks the test** (per Q4).
- Network unavailable during template fetch → skill uses cache from prior run if available, else fails clearly.

### Install / chat edge cases

- `zombiectl install` fails with non-zero exit → skill prints stderr verbatim, exits without opening chat.
- User Ctrl-C's during chat → CLI exits cleanly, zombie keeps running (per M37_001 step 9).
- User re-runs `/usezombie-install-platform-ops` after a successful install → skill detects existing `.usezombie/platform-ops/`, asks "update or skip?" (does NOT silently overwrite without `--force`).

### Template-fetch edge cases (per A5)

- Cache hit, but cache version older than skill's pinned tag → skill refetches.
- Cache hit, network down, version matches → use cache (offline mode works).
- Cache miss, network down → fail clearly with "no template cached and unable to fetch — try again when online."
- Pinned tag deleted on GitHub (rare) → fall back to `main`, warn user that the version is unverified.

## Critical Paths (must work end-to-end before launch)

1. **Cold install on Customer Zero's broken dev CD** — Day 1-3 dogfood. The launch demo screenshot.
2. **Cold install + first chat against hosted zombid** — the default (and v2-only) OSS user experience. Should complete in <60 seconds from skill invocation to first Slack post.
3. ~~**Cold install + first chat in self-host mode on clean Ubuntu 24.04**~~ [REVISED Apr 25: removed. Self-host arrives in v3; no v2 launch-blocking validation pass on a vanilla Linux host.]
4. **Re-run idempotency** — running the skill twice on the same repo doesn't break anything. Refuses without `--force`.
5. **Secret-leak guardrail** — gitleaks scans every generated file in CI. Zero matches required.
6. **Multi-agent compatibility smoke** — same skill produces equivalent files in Claude Code, Codex CLI, OpenCode, Amp. Equivalence = same `.usezombie/platform-ops/SKILL.md` byte-for-byte after running through each host (verifies the no-per-host-fork claim).
7. **Eval suite (in `usezombie/skills-evals`)** — given 10 fixture broken-CD logs, the platform-ops zombie's diagnosis names the right symptom ≥7/10 of the time.
8. **Context-lifecycle smoke** — a zombie that runs 25 tool calls in one stage gracefully uses memory_store at 5-call intervals (per A8 mechanism #2). Verify memory entries appear in `core.zombie_events` log.
9. [ADDED Apr 25] **BYOK provider key resolution** (M48) — operator stores Anthropic / OpenAI / Together / Groq key via `zombiectl credential add`; install-skill picks it up; first steer routes inference through the operator's key, not the platform LLM. Verify on at least 2 providers.
10. [ADDED Apr 25] **`/self-host` returns 404** — verifies the page is intentionally absent in v2; no stale stub leaks.

## Regression Surface (must not break)

- M37_001 platform-ops sample (`samples/platform-ops/{SKILL,TRIGGER,README}.md`) is byte-identical before and after install — the skill consumes, never modifies.
- M19_003 `zombiectl install --from <path>` behavior unchanged.
- Existing CLI commands (`zombiectl credential add`, `zombiectl list`, `zombiectl status`, `zombiectl kill`, `zombiectl logs`, `zombiectl doctor`, `zombiectl install`) behaviors unchanged. NEW commands shipped in M33_001 substrate: `zombiectl steer {id}` (CLI did not exist before), and extended behavior on `zombiectl doctor` (now also checks workspace binding, per A9 resolution).
- `core.zombie_events` schema unchanged.

## Test Infrastructure

- **Fixture repo**: `samples/fixtures/platform-ops-target/` — minimal repo with `.github/workflows/deploy.yml` + `fly.toml` + `README.md`. Used by integration test.
- **CI job**: `.github/workflows/test-install-skill.yml` — runs the skill against the fixture in a headless mode, asserts generated files, runs gitleaks scan.
- **Eval job**: lives in `usezombie/skills-evals` repo. Runs nightly. Score threshold gates skill-repo releases.
- **Manual smoke**: clean Ubuntu 24.04 VM (DigitalOcean droplet $4/mo) used for the A3 launch-validation pass. Document the exact commands run.
