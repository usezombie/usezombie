Oh, so here's my thought. I feel GitIt is a source of truth. It's sort of like a database. I store all my artifacts in there. It's able to successfully manage my artefacts. The end of the The day. I need my artifacts to be packaged and pushed deployed to some sort of infra. The infra could be anything. I might consume AWS, OVH or anything. So as an end user I would probably get into something, a system and then basically Speak through the system saying here is my artifact in Git. But I'm giving you For dev, here's my workspace that I logged in. You were supposed to break all my specs, build the software. And also deploy. The enterprise instructions are in your file here. So the first job of use zombie should be to Just dissect all that for my workspace environment. Produce the artefacts needed for an agent with acceptance criteria. So it's more of producing the harness layer. And start handing off to agents in parallel, so it produces an output train. When there's an error, it would help to capture the observability and see what's happening. 